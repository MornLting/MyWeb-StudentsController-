@{
    ViewBag.Title = "添加专业";
}

@model MajorViewModel

<form method="post" class="mx-auto" style="windth:320px;">
    <div asp-validation-summary="All" class="has-text-danger mb-4"></div>
    <div class="field">
        <label class="lable" asp-for="Name"></label>
        <div class="control">
            <input asp-for="Name" class="input" />
        </div>
    </div>
    <div class="field">
        <button class="button is-primary is-fullwidth">提交</button>
    </div>
</form>
